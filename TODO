To be covered (tentative):

    Microservice Architecture -> Slides
    API? -> Slides
    API Terminology -> Slides

    Hello World -> Flask
    
    Render Template -> Flask

    Docker -> Slides
    Dockerize this Flask app

    Consume API -> Python

    Rest API Structure -> Slides
    Common Headers -> Slides

    Parse header -> Flask
    Send header -> React
    Send header -> Python

    Database in Docker -> Demo

    Login -> Flask

    Some common login methods -> Slides

    Middleware -> Slides
    Middleware example -> Django/golang

    Decorators in Flask (as per the time)